# Firmware

Ten katalog zawiera projekty mikrokontrolerów należące do robota.

- `STM32G474VET6-module-board/` — główny projekt sterownika STM32CubeIDE.

Nie umieszczaj tutaj katalogów roboczych CubeIDE ani wygenerowanych katalogów
`Debug` i `Release`.
